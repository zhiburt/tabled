[package]
name = "colored_backend"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
papergrid = { path = "../../", features = ["color"] }
owo-colors = "3.4.0"
